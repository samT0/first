<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.yc.dao.IGoodsInfoMapper">
 	<!-- 添加商品信息 -->
 	<select id="add" resultType="com.yc.po.GoodsInfo">
 		insert into goodsInfo values(null,#{gname},#{tno},#{price},#{intro},#{balance},#{pics},#{unit},#{qperied},#{weight},#{descr})
 	</select>
 	
 </mapper>